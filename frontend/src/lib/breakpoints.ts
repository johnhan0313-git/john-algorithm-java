/** Tailwind `lg` — tablet/desktop layout from here up */
export const DESKTOP_MIN_WIDTH = 1024;

export const DESKTOP_LAYOUT_MQ = `(min-width: ${DESKTOP_MIN_WIDTH}px)`;
export const DRAWER_LAYOUT_MQ = `(max-width: ${DESKTOP_MIN_WIDTH - 1}px)`;
